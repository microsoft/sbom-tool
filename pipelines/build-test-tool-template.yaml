steps:
  - task: UseDotNet@2
    displayName: 'Use .NET Core'
    inputs:
      useGlobalJson: true

  - task: DotNetCoreCLI@2
    displayName: 'Restore solution'
    inputs:
      command: restore
      feedsToUse: config
      nugetConfigPath: nuget.config
      verbosityRestore: Normal

  - task: DotNetCoreCLI@2
    displayName: Build
    inputs:
      arguments: '-c $(BuildConfiguration)'

  - pwsh: |
      dotnet test -c $(BuildConfiguration) --no-build -- --report-trx --results-directory $(Build.ArtifactStagingDirectory)/artifacts/TestResults --coverage --coverage-output $(Build.ArtifactStagingDirectory)/artifacts/Coverage/coverage.cobertura.xml --coverage-output-format cobertura
    displayName: Run unit tests

  - task: PublishTestResults@2
    displayName: Publish TRX Test Results
    condition: eq(variables['Agent.OS'], 'Windows_NT')
    inputs:
      testResultsFormat: 'VSTest'
      testResultsFiles: '*.trx'
      searchFolder: '$(Build.ArtifactStagingDirectory)/artifacts/TestResults'
      testRunTitle: 'Unit Tests for $(BuildConfiguration)'
      mergeTestResults: true
    continueOnError: true
    condition: always()

  - task: PublishCodeCoverageResults@2
    displayName: "Publish code coverage (Windows only)"
    condition: eq(variables['Agent.OS'], 'Windows_NT')
    inputs:
      summaryFileLocation: '$(Build.ArtifactStagingDirectory)/artifacts/Coverage/coverage.cobertura.xml'
